# WORKFLOW.md
## กระบวนการผลิต — ลำดับงานและด่านตรวจ

- **อะไร** → [PLAN.md](PLAN.md)
- **สร้างอย่างไร (กฎ)** → [BUILD_RULES.md](BUILD_RULES.md)
- **ทำตามลำดับไหน (เอกสารนี้)** → WORKFLOW.md

**หลักการเดียวของเอกสารนี้:** ทุกขั้นมี **ด่าน (Gate)** ที่ต้องผ่านก่อนไปขั้นถัดไป ด่านมีไว้เพื่อไม่ให้ความผิดพลาดราคาถูกกลายเป็นความผิดพลาดราคาแพง

---

## ส่วนที่ 1 — Workflow ของทั้งคอร์ส

```
PHASE 0  Setup                    ทำครั้งเดียว
   │     tools + templates + shell + course_outline + status
   ▼
PHASE 1  สัปดาห์ 1 = ต้นแบบ        ทำจนผ่าน G8 แล้ว FREEZE รูปแบบ
   │     ◄── จุดเดียวที่อนุญาตให้เปลี่ยนรูปแบบเอกสาร
   ▼
PHASE 2  สัปดาห์ 2 → 3 → 4 → 5     ตามลำดับ ห้ามข้าม
   │     แต่ละสัปดาห์วน W-0 … W-8
   ▼
PHASE 3  Final exam                หลังเนื้อหาทั้ง 5 สัปดาห์นิ่งแล้ว
   │     blueprint → เขียนข้อ → verifier → dry-run import
   ▼
PHASE 4  Integration audit         ตรวจทั้งคอร์สเป็นก้อนเดียว
```

### PHASE 0 — Setup (ทำครั้งเดียว)

| ลำดับ | งาน | ตรวจอะไร |
|-------|-----|---------|
| 0.1 | `python tools/build_slides.py --make-shell` | ได้ `templates/slide_shell.html` |
| 0.2 | ทดสอบ build ด้วยสไลด์ตัวอย่าง 1 ไฟล์ แล้วเปิด `.html` จริง | **สมการเรนเดอร์** ไม่มี `$` ค้างบนจอ · ลูกศร ←/→ เลื่อนสไลด์ได้ |
| 0.3 | `python tools/verify_math_exam.py exams/final_items_example.json` | ได้ `RESULT: PASSED` |
| 0.4 | `python tools/audit_figures.py` | รันผ่าน (ยังไม่มีภาพก็ไม่เป็นไร) |
| 0.5 | เขียน `course_outline.md` (ฉบับสำหรับนักศึกษา) + `status.md` | |

> **Gate G-Setup:** เครื่องมือทั้ง 3 ตัวรันได้จริงบนเครื่องนี้ ก่อนเขียนเนื้อหาชิ้นแรก
> เหตุผล: ถ้า build พังตอนสัปดาห์ 3 จะมีสไลด์ค้างท่อ 8 ชุด

### PHASE 1 — สัปดาห์ 1 เป็นต้นแบบ

ทำสัปดาห์ 1 ตาม W-0 … W-8 ให้ครบ **แล้วหยุดทบทวนรูปแบบ**

> **Gate G-Freeze:** ตอบให้ได้ว่า — โครงสไลด์แบบนี้ใช้ได้ไหม · ระดับความละเอียดของเฉลยพอไหม · สัดส่วนแบบฝึกหัด 60/30/10 เหมาะไหม · type ของภาพแบ่งถูกไหม
>
> **หลังผ่านด่านนี้ ห้ามเปลี่ยนรูปแบบเอกสารอีก** ถ้าเปลี่ยนตอนสัปดาห์ 4 ต้องย้อนแก้สัปดาห์ 1–3 ทั้งหมด

### PHASE 2 — สัปดาห์ 2 ถึง 5

**ห้ามข้ามลำดับ** เพราะทุกสัปดาห์มี forward reference ที่สัปดาห์หลังต้องปิด — เขียนสัปดาห์ 5 ก่อนสัปดาห์ 3 จะไม่รู้ว่าต้องปิดอะไร

**กฎการย้อนกลับ:** ถ้าเขียนสัปดาห์ N แล้วพบว่าต้องแก้สัปดาห์ M < N
1. แก้สัปดาห์ M
2. **รัน Gate G8 ของสัปดาห์ M ใหม่ทั้งด่าน** (ไม่ใช่แก้แล้วผ่านเลย)
3. ตรวจว่าแบบฝึกหัด/Quiz/ภาพ ที่อ้างถึงสัปดาห์ M ยังตรง
4. บันทึกการย้อนกลับใน `status.md`

### PHASE 3 — Final exam

| ลำดับ | งาน |
|-------|-----|
| 3.1 | เขียน `exams/final_blueprint.md` — จำนวนข้อต่อสัปดาห์ต่อระดับ Bloom **ก่อนเขียนข้อแรก** |
| 3.2 | เขียนข้อลง `exams/final_items.json` ตามกฎ SA-1 … SA-11 |
| 3.3 | `python tools/verify_math_exam.py exams/final_items.json` จนได้ PASSED |
| 3.4 | ทำโจทย์เองทั้งฉบับด้วยมือ จับเวลา — เวลาที่ใช้ × 3 = เวลาสอบที่ควรให้ |
| 3.5 | import ด้วย `dry_run=true` ดู preview → แก้ → `dry_run=false` |

> **Gate G-Exam:** verifier PASSED · ทำเองครบทุกข้อแล้วคำตอบตรงกับ `options` ทุกข้อ · ไม่มีข้อไหนที่ทำแล้วรู้สึกว่า "ต้องเห็นวิธีทำจึงให้คะแนนได้"
>
> ข้อ 3.4 สำคัญ — ข้อสอบตอบสั้นดูสั้นแต่คำนวณด้วยมือใช้เวลาจริง ประเมินจากการอ่านโจทย์อย่างเดียวจะพลาดทุกครั้ง

### PHASE 4 — Integration audit

- [ ] **แผนที่การปิดวง** ใน `PLAN.md` ทุกเส้นปิดจริง — เปิดสไลด์ที่อ้างถึงดูทีละเส้น
- [ ] `python tools/audit_figures.py` — missing = 0 และสัดส่วน type ไม่ผิดทิศ
- [ ] เวลารวมทุกสัปดาห์ = **180 นาทีพอดี × 5**
- [ ] `index.html` ลิงก์ครบทุกชุดและเปิดได้จริง
- [ ] คำศัพท์เทคนิคใช้คำเดียวกันทั้งคอร์ส (เช่น เลือกใช้ "ผลเฉลย" หรือ "คำตอบ" อย่างใดอย่างหนึ่ง แล้วใช้ให้ตรงกัน)
- [ ] ตารางส่งมอบ (Handoff) เทียบกับ syllabus จริงของคอร์ส 15 สัปดาห์แล้ว

---

## ส่วนที่ 2 — Workflow ของแต่ละสัปดาห์ (W-0 … W-8)

```
W-0 เตรียม ─► W-1 ร่างโครง ─► W-2 ออกแบบตัวเลข ─► W-3a NOTE ─► W-3b สไลด์
                                                            │
                     W-8 ปิด ◄── W-7 Quiz ◄── W-6 แบบฝึกหัด ◄┤
                                                            │
                              W-5 audit ภาพ ◄── W-4 build ◄──┘
```

### W-0 · เตรียม (15 นาที)

1. อ่านตาราง 180 นาทีของสัปดาห์นั้นใน `PLAN.md`
2. เปิด **แผนที่การปิดวง** จดสองรายการ: สัปดาห์นี้ต้อง **ปิด** อะไร · ต้อง **ตั้งค้าง** อะไร
3. เขียนสองรายการนั้นลง `week_0N_*/README.md` ทันที

> **Gate G0:** รายการ "ปิด/ตั้งค้าง" ถูกเขียนลงไฟล์แล้ว ก่อนเขียนสไลด์บรรทัดแรก

### W-1 · ร่างโครง — เขียนแค่หัวข้อ ยังไม่เขียนเนื้อหา

1 ช่วงในตาราง = 1 ชุดสไลด์ · ในแต่ละชุดเขียนแค่:

```
## Slide N — ชื่อสไลด์
**Key Message**: ประโยคเดียวที่จบความ
```

> **Gate G1:** 6–23 สไลด์/ชุด (กฎ SL-1a) · Key Message ทุกอันเป็น **ประโยค** ไม่ใช่หัวข้อ · โครงครอบคลุมรายการจาก G0 ครบ
>
> เหตุผลที่แยกขั้นนี้ออกมา: ถ้าเขียนเนื้อเสร็จแล้วพบว่าลำดับผิด งานที่ต้องทิ้งคือทั้งชุด แต่ถ้าพบตอนยังเป็นโครง แก้ 5 นาที

### W-2 · ออกแบบโจทย์และตัวเลข — ขั้นที่มักถูกข้าม และแพงที่สุดถ้าข้าม

**ก่อนเขียนสไลด์** เลือกตัวเลขของทุกตัวอย่างและ **คำนวณด้วยมือจนจบ** เก็บผลไว้ในที่ทำงาน

เกณฑ์รับ:
- ทำมือจบใน **ไม่เกิน 3 บรรทัด**
- คำตอบเป็นจำนวนเต็ม หรือเศษส่วนอย่างต่ำ
- **ไม่ต้องเปิดตาราง $e^x$ หรือ $\log$** — ถ้าต้องเปิด ออกแบบเลขใหม่

> **Gate G2:** ทุกตัวอย่างในสัปดาห์นี้มีคำตอบที่คำนวณแล้วจริง ไม่ใช่ "เดี๋ยวคิดตอนเขียน"
>
> เหตุผล: การเปลี่ยนตัวเลขหลังสไลด์เสร็จ ต้องตามไปแก้ **สไลด์ + เฉลยแบบฝึกหัด + Quiz + ข้อสอบ** ที่อ้างเลขชุดเดียวกัน

### W-3a · เขียน Lecture Note (ทำก่อนสไลด์)

`week_0N_*/notes/note_0N.md` — เอกสารร้อยแก้วที่อ่านจบได้เองโดยไม่ต้องดูสไลด์

- ทุกหัวข้อย่อยต้องมี **ตัวอย่างทำมือ 3 ตัว ไล่ระดับ** ตามกฎ NT-4a: ตรงไปตรงมา → มีชั้นเชิง → ผลลัพธ์ผิดคาด
- ใช้ตัวเลขที่คำนวณไว้แล้วจาก W-2
- ใส่ marker ภาพตามกฎ FG-1 — ยังไม่ต้องวาด

> **Gate G3a:** ผ่านกฎ NT-1 … NT-10 · **นับตัวอย่างได้ ≥ 3 ต่อหัวข้อย่อยจริง** · ยาว 12–18 หน้า A4
>
> **ทำไม note มาก่อนสไลด์:** เนื้อหาคณิตศาสตร์ต้องคิดจนจบเป็นร้อยแก้วก่อน ถ้าเขียนสไลด์ก่อน สไลด์จะกลายเป็นหัวข้อที่ยังไม่ได้คิดจนจบ และ note จะกลายเป็นงานที่ทำทีหลังแบบเสียไม่ได้ · เขียน note ก่อนแล้วตัดเป็นสไลด์เป็นงานเชิงกลไก

### W-3b · ตัด note เป็นสไลด์

- เติมเนื้อลงโครงจาก W-1 โดย **คัดจาก note** ไม่คิดใหม่
- ตัวอย่างที่ 1 และ 2 ของแต่ละหัวข้อขึ้นสไลด์ · **ตัวอย่างที่ 3 เก็บไว้ใน note เท่านั้น** (กฎ NT-6)
- ใช้ marker ภาพ **id เดียวกับใน note** ถ้าเป็นภาพเดียวกัน (กฎ FG-3) ไม่วาดสองรอบ

> **Gate G3b:** ผ่านกฎ SL-1 … SL-10 · ทุกสไลด์มี Key Message · ไม่มีสมการใน ``` code fence · สไลด์ไม่ขัดกับ note

### W-4 · build และตรวจการเรนเดอร์

```bash
python tools/build_slides.py week_0N_*/slides/*.md
```

แล้ว **เปิดไฟล์ `.html` ดูด้วยตาจริง**

> **Gate G4:** ไม่มี warning ค้าง · สมการเรนเดอร์ครบ (ไม่เห็น `$` บนจอ) · ตารางไม่ล้นขอบสไลด์
>
> ข้อ "เปิดดูจริง" ไม่ใช่พิธีกรรม — สมการที่พิมพ์ผิดจะไม่เรนเดอร์และ build ไม่ error

### W-5 · audit ภาพ

```bash
python tools/audit_figures.py
```

แล้วทำภาพที่ยังขาด โดยเลือกวิธีทำตาม **type** (ดู BUILD_RULES หมวด 6.5)

> **Gate G5:** audit รายงาน `missing = 0` และ id ไม่ซ้ำ

### W-6 · แบบฝึกหัดและเฉลย

> **Gate G6:** ผ่านกฎ EX-1 … EX-7 · เฉลยแสดงทุกบรรทัด · **ทำโจทย์เองทุกข้อแล้ว** ไม่ใช่เขียนเฉลยจากความจำ

### W-7 · Quiz (สัปดาห์ 2–5 เท่านั้น)

> **Gate G7:** `verify_math_exam.py` PASSED · ทุกข้อบอกรูปแบบคำตอบในวงเล็บ

### W-8 · ปิดสัปดาห์

1. เขียน/อัปเดต `week_0N_*/README.md`
2. อัปเดต `status.md`
3. รัน QC checklist (BUILD_RULES หมวด 8) ทั้งชุด

> **Gate G8:** QC ผ่านทุกข้อ → สัปดาห์นี้ปิด ขึ้นสัปดาห์ถัดไปได้

---

## ส่วนที่ 3 — สรุปด่านทั้งหมด

| ด่าน | อยู่ที่ | ตรวจอะไร | ถ้าไม่ผ่านจะเสียอะไร |
|------|--------|---------|---------------------|
| G-Setup | Phase 0 | เครื่องมือรันได้จริง | สไลด์ค้างท่อทั้งคอร์ส |
| G-Freeze | Phase 1 | รูปแบบเอกสารนิ่ง | ต้องย้อนแก้ทุกสัปดาห์ที่ทำไปแล้ว |
| G0 | W-0 | รายการปิด/ตั้งค้าง | forward reference ไม่ปิด ผู้เรียนค้างคา |
| G1 | W-1 | โครงและ Key Message | ทิ้งงานทั้งชุด |
| G2 | W-2 | ตัวเลขคำนวณแล้ว | ต้องแก้ 4 ที่พร้อมกัน |
| G3a | W-3a | กฎ note · ตัวอย่าง ≥3/หัวข้อ | เนื้อหาไม่ครบ นักศึกษาอ่านเองไม่ได้ |
| G3b | W-3b | กฎสไลด์ | สไลด์ไม่สม่ำเสมอทั้งคอร์ส |
| G4 | W-4 | การเรนเดอร์จริง | สมการหายกลางคาบสอน |
| G5 | W-5 | ภาพครบ | สไลด์เป็นตัวหนังสือล้วน |
| G6 | W-6 | แบบฝึกหัดและเฉลย | เฉลยผิด นักศึกษาเสียความเชื่อถือ |
| G7 | W-7 | Quiz ผ่าน verifier | ตรวจอัตโนมัติผิด คะแนนผิด |
| G8 | W-8 | QC ครบ | หนี้เทคนิคสะสม |
| G-Exam | Phase 3 | ข้อสอบและเวลาสอบ | ข้อสอบยาวเกินเวลา |
